services:
  - type: web
    name: task-management-api
    runtime: bun
    plan: free
    buildCommand: bun install
    startCommand: bun src/server.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false
